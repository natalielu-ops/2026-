# audio/

背景音樂放在這個資料夾。

1. 把歌曲檔案命名為 `our-song.mp3`，放進這個資料夾。
2. 如果想用別的檔名，改 `js/content.js` 裡 `CONTENT.music.src` 就好。

歌名、歌手、音量、是否重複播放也都在 `js/content.js` 的 `CONTENT.music` 設定。

支援的格式：`.mp3`、`.m4a`、`.ogg`、`.wav`（一般用 mp3 最保險，所有瀏覽器都能放）。

> 檔案建議控制在 10 MB 以內，GitHub Pages 載入會比較快。
> 網頁是公開的，請只放自己有權利散布的音檔。
